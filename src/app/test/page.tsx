import InstagramCarousel from "~/components/InstagramCarousel";

export default function TestPage() {
  return (
    <main className="flex h-full w-full flex-col items-center justify-center p-12">
      <InstagramCarousel />
    </main>
  );
}
